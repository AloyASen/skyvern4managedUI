"""Utility scripts packaged for runtime use.

This package exposes helper entrypoints like
`scripts.create_organization` so they can be executed via
`python -m scripts.create_organization` in production images.
"""

